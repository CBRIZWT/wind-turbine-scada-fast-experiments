# GitHub Report：风机 SCADA 快速实验

## 1. 项目定位

本模块用于快速筛选温度异常检测/预测方案、比较基线、验证事件级指标和评估真实故障管线接线。它服务于研究决策，不等同于最终论文实验或 SOTA 排名。

建议仓库：`wind-turbine-scada-fast-experiments`

## 2. 当前可公开内容

- `快速实验/` 中的运行器、数据工具、汇总器、可视化和基础模型源码。
- `快速实验/基础模型/` 中可独立复核的模型实现；去除生成缓存和训练产物。
- `tests/test_fast_*.py`、`tests/test_real_fault_*.py`、`tests/test_metrics_*.py` 等快速实验、真实故障和指标契约测试。
- `docs/真实故障事件级快速实验报告_2026-07-11.md`、快速实验设计文档及脱敏后的运行说明。
- 经核验的汇总表，而不是完整原始日志或未经审计的排行榜。

## 3. 当前实验解释

| 类型 | 证据等级 | 发布规则 |
|---|---|---|
| smoke/dry-run | 接线/局部行为 | 只能作为运行示例 |
| 单种子快速实验 | 探索性筛选 | 必须标注单种子、事件数和 split |
| LOEO/真实故障快速实验 | 有限事件级证据 | 必须提供 farm、事件、标签和协议 |
| 7 天命中窗口设计 | 设计状态 | 不得写成已完成的 7 天重跑结果 |
| Hill 无标签协议 | external unlabeled | 只能报告分数/报警负担，不做分类性能排名 |

已有结果如 MAE、RMSE、AUPRC 等必须从对应 manifest 和 metrics 文件重新抽取；报告不能只引用脚本名或终端输出。

## 4. 建议公开目录

```text
README.md
quick_experiments/
models/
evaluation/
tests/
docs/
configs/
results_summary/
```

README 应明确：快速实验用于筛选；原始数据、缓存、权重和完整运行日志不公开；结果不代表确认性结论。

## 5. 明确排除

- `快速实验/快速实验数据/` 中的 `.npy/.npz` 和其他缓存。
- `实验结果/` 中的日志、检查点、临时输出和未审计排行榜。
- `快速实验/基础模型/catboost_info/` 等运行时生成目录。
- 含真实 SCADA 记录、风场私有字段或可回溯设备信息的文件。
- 任何将本地 `E:\创新`、`E:\ancoda\chuangxin` 固化到代码中的配置。

## 6. 复现入口与限制

推荐流程：安装依赖 → 准备合法数据 → 执行小规模 smoke → 核验 manifest → 再运行快速实验。预测步长、故障前命中窗口、标签版本和 `preprocess_variant` 必须分开记录，不能把扩大命中窗解释为改变预测步长。

## 7. 公开前门禁

- [ ] 所有结果均注明 seed、farm、事件数、指标和协议。
- [ ] LogLoss 使用原始 `P(y=1)`，概率语义未被阈值极性混淆。
- [ ] Hill 无标签结果未被放入分类性能排名。
- [ ] 未把探索性榜单称为最终 benchmark。
- [ ] 运行器默认不读取公开仓库之外的私有绝对路径。
